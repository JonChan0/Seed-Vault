[project]
name = "seed-vault"
version = "2.0.0"
requires-python = ">=3.11"
dependencies = [
    "opendataloader-pdf>=2.0",
    "pypandoc>=1.14",
    "python-frontmatter>=1.1",
    "pyyaml>=6.0",
    "thefuzz>=0.22",
]
